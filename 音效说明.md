# 贵阳捉鸡麻将 · 音效设计说明

> 本文说明 **index.html（单文件游戏）** 里音效系统的设计、每一条音效的触发时机与音色、
> 加载与播放方式、实现代码位置，以及"想换成真实录音 / 想调音色"该怎么改。
> 全部代码已内联在 `index.html`，不需要任何附加文件。

---

## 一、总体方案：为什么是"实时合成"而不是一堆 mp3

| 约束 | 说明 |
|---|---|
| **单文件发布** | 整个游戏就是一个 `index.html`，拷到手机双击就能玩 |
| **`file://` 打开** | Chrome / Edge 在 `file://` 下**禁止 fetch/XHR 读本地文件**（跨源），"预加载一批 mp3 再播放"这条路在本地直接走不通 |
| **离线 / 体积** | 合成音**零资源**：不附带文件、离线可用、永不 404、不占体积 |
| **可调** | 每条音效是一个"配方"，改音色只改参数，不用重新找素材 |

因此采用 **WebAudio 实时合成**，并留了一条"以后想换成真实录音"的口子（见第五节）。
三级降级链，任何一级不可用都不影响牌局：

```
真实样本（放了 sfx/ 文件才会有） → WebAudio 合成 → 静默空转（没有 WebAudio / 被静音 / 创建失败）
```

---

## 二、音效清单（23 条）

`hit` 类音色 = 白噪声过带通 + 4ms 起振 + 极快衰减（麻将的「嗒 / 啪」就是这个形状）；
`note` 类音色 = 三角波柔音，可滑音（长时间听不刺耳）。

### A. 操作与提示类

| # | 音效名 | 触发时机（函数） | 音色设计 | 配方构成 |
|---|---|---|---|---|
| 1 | `tap` | `onTileClick` 预选那张牌；各按钮（确认名字 / 全屏 / 展开·关闭结算窗 / 本局详情 / 音效开关） | 木珠轻碰 | 2 响（1500Hz + 2600Hz，错开 6ms） |
| 2 | `deny` | `onTileClick` 点到"点不动"的牌（不是自己回合 / 有动作待决） | 低闷「唔」下行 | 1 音（196→150Hz） |
| 3 | `discard` | `discard()` 出牌（四家都响） | 牌拍桌面：高频碎裂 + 低频冲击 | 2 响（1800Hz / 420Hz 低通） |
| 4 | `draw` | `drawTile()` 摸牌（四家都响，很轻） | 牌从牌墙滑出 | 2 响（3200Hz 高通 + 1500Hz） |
| 5 | `prompt` | `drawTile()` 轮到玩家且无牌可做 | 柔和两音，不催人 | 2 音（784 / 1047Hz） |
| 6 | `alert` | `autoGangHint()` 有牌可做（可自摸/暗杠/补杠）；`setActions()` 有碰/杠可选 | 三连上行叮 | 3 音（880/1175/1568Hz） |
| 7 | `huReady` | 能胡（含可抢杠胡）；`setActions()` 里含 `type==='hu'` 时 | 五音上行琶音（全套最亮） | 5 音（784→2093Hz） |
| 8 | `ting` | `discard()` 后玩家**刚刚叫牌**（`G.tingFlag` 只在翻转那次响） | 清脆金铃（三度上行） | 2 音（988 → 1319Hz） |
| 9 | `dice` / `diceStop` | `runDiceUI()` 骰子滚动 / 落定 | 碗里连响 + 落定清脆一响 | 7 响 + 1 滑音 / 2 响 |

### B. 动作类（碰杠胡）

| # | 音效名 | 触发时机（函数） | 音色设计 | 配方构成 |
|---|---|---|---|---|
| 10 | `peng` | `doPeng()` | 三声「嗒嗒嗒」= 三张牌码齐（音高逐张微升） | 3 响（1250/1340/1430Hz） |
| 11 | `gang` | `doGang()` 明杠 | 四声、更矮更重 | 4 响（950Hz 起，逐张 +70） |
| 12 | `angang` | `doAnGang()` 暗杠 | 闷响（牌扣着敲桌：低通 + 低音铺底） | 2 响低通 + 1 音（147→110Hz） |
| 13 | `bugang` | `doBuGang()` 补杠（爬坡豆） | 中音双响「再接一张上来」 | 2 响（1050 / 1400Hz） |
| 14 | `hu` | `doHu()` 点炮胡（牌型 <17 番） | 温和四音上行 | 4 音（523→1047Hz） |
| 15 | `zimo` | `doHu()` 自摸胡（牌型 <17 番） | 六音上行 + 长尾 | 6 音 + 1 收尾音 |
| 16 | `huBig` | `doHu()` 牌型 **≥17 番**（清一色 / 龙七对 / 大对子…，自摸点炮都用） | 自摸的加强版 + 低音铺底，明显更"大" | 7 音 + 2 音铺底 |
| 17 | `qianggang` | `checkQiangGang()` 电脑抢杠胡 | 两声急促上冲（方波更"冲"） | 1 响 + 2 音（880/1760Hz） |
| 18 | `hotcannon` | `doHu()` 且本张为**热炮**（杠上炮） | 上行后急转直下 ——「烧掉了」 | 3 音（1200→120Hz） |

### C. 流程类

| # | 音效名 | 触发时机（函数） | 音色设计 | 配方构成 |
|---|---|---|---|---|
| 19 | `deal` | `dealByDice()` 发牌（一次 53 张） | 一串快速摩擦「唰」 | 9 响 + 1 低频 |
| 20 | `chicken` | `settle()` 翻鸡牌（**延后 0.75s** 起播） | 两下"咯咯"感 + 高音叮 | 2 响 + 2 音 |
| 21 | `settle` | `showResult()` 结算窗弹出（**延后 1.1s**） | 轻快两音 | 2 音（659/988Hz） |
| 22 | `liuju` | `endGame()` 荒庄 | 下行两音（泄气但不刺耳） | 2 音（523 → 294Hz） |

### 关键：三个音的**时序编排**

一局胡牌要播三个音，如果同时响会糊成噪音。所以刻意排成前后：

```
0.00s  胡牌音      hu / zimo / huBig        （立刻响，1.2 秒长音）
0.55s  hotcannon   仅热炮时叠一段"烧掉"的下行音
0.75s  翻鸡音      chicken                  （落在胡牌音的尾巴上）
1.10s  结算窗音    settle                    （窗弹出时那一声轻快两音）
```

---

## 三、加载与播放方式（三条工程要点）

### 1. 懒创建 + 手势解锁（绕开浏览器的自动播放限制）

浏览器要求"先有用户手势"才允许出声。做法：

- `AudioContext` **不在页面加载时创建**，第一次真要发声才 `new`（一进页面就建会被判成打扰用户）；
- 首次 `pointerdown` / `touchstart` / `keydown` 自动 `resume()` 解锁，牌桌上的任何点击也都是手势；
- 还没有解锁时，音效**照样排进时间轴**（`currentTime` 在挂起时不前进，恢复那一刻就会响），
  所以"第一下点击"不会白点。

### 2. 节流（防止连点叠成噪音）

```js
const THROTTLE={tap:55,draw:20,discard:0,deal:0,dice:0};
```

同一音效在窗口期内的重复调用直接丢弃。实测连打 10 次 `tap` 只出 2 声。
只有"高频重复一定没意义"的几个 key 设了限流，其余（碰/杠/胡…）不限，保证动作不被吞。

### 3. 切后台挂起（移动端省电）

`visibilitychange` 时 `suspend()`，回前台再 `resume()`。移动端很重要 ——
页面切走之后还留着音频硬件开着是白耗电。

### 4. 静音开关（持久化）

- 状态存 `localStorage['gymj.sfx.v1']`（`{on:true,vol:0.75}`），刷新后保持；
- 关掉是**真的不排音频节点**（不是把音量调 0），实测关掉后节点数为 0；
- 按钮在**右下角**（`#sndBtn`，喇叭图标，关闭时显示斜杠 + 半透明）；
  浏览器不支持 WebAudio 时按钮**自动隐藏**——不给一个点了没反应的假开关。

> ⚠️ 这个按钮的位置是**实测选出来的**，别凭直觉挪：右上角试了 3 个位置分别压到
> 「对家手牌背面」「上家名字标签」，左下角压「下家手牌」，右下角往上挪压「自己的手牌」。
> 最后把 12 个候选位置逐个跑布局回归，采用 **右下角贴边** +
> **极矮横屏（≤380px 高）往里让开 52px** 两条规则（原因见代码里的注释）。

---

## 四、实现代码在哪

全部内联在 `index.html` 的 `<script>` 里，一共三块：

### ① 音效引擎（搜索 `音效引擎 SFX`）

```js
const SFX=(function(){
  // 配置（静音/音量，存 localStorage）
  // ensure()  —— 懒创建 AudioContext + master 增益 + 一份 0.4s 白噪声缓冲（所有撞击声共用）
  // unlock()  —— 首次手势解锁
  // hit(t,f,o)   —— 发声原语 1：白噪声 → 带通 → 4ms 起振 / 极快衰减（麻将的「嗒/啪」）
  // note(t,f,d,o)—— 发声原语 2：三角波柔音，可滑音
  // R={...}   —— 23 条音效的配方表（t = 起始时刻）
  // play(name,opt) —— 节流 + 延迟(delay) + 静默保护
  // setOn/toggle/on/available —— 开关与能力探测
  // SAMPLES/loadSamples() —— 可选：换成真实录音
  return {...};
})();
```

- 所有对外调用都包在 `try/catch` 里，任何异常都**静默忽略**，绝不影响牌局逻辑；
- 挂点全部写成 `SFX.play('名字')`，共 **30 处**（含胡牌的 3 个分支与 2 处三元）。

### ② 音效开关按钮

- HTML：`#sndBtn`（与 `#fsBtn` 同款圆形，位置在右下角）；
- CSS：`#sndBtn{...}` + `body.sndOff #sndBtn{...}` + `@media (max-height:380px)`；
- JS：`initSoundToggle()` —— 同步图标/提示语、点击切换并落盘；不支持 WebAudio 就隐藏按钮。

### ③ 挂点（在每个事件处理函数里一行）

例：

```js
function discard(p,card){
  G.hands[p][card]--;G.discards[p].push(card);
  SFX.play('discard');   // 出牌：牌拍在桌面上的那一声（四家出牌都响）
  ...
}
```

---

## 五、想换成真实录音（不用改任何调用处）

1. 在 `index.html` 同目录建 `sfx/`，放入同名文件：
   `discard.mp3 / peng.mp3 / gang.mp3 / hu.mp3 / …`（名字就是音效名，见第二节表格）；
2. 在引擎里填上映射表（现在默认为空）：

```js
const SAMPLES={
  discard:'sfx/discard.mp3',
  peng   :'sfx/peng.mp3',
  hu     :'sfx/hu.mp3',
  // …按需加
};
```

3. 加载成功 → 该音效改播样本；失败 → 静默回落到合成音（**`file://` 下必然失败，
   所以本地打开时全部走合成音，这是预期行为**）。

> 为什么用 `<audio>` 而不是 `fetch` 预加载：`file://` 下 `fetch` 被跨源拦截，
> 而 `<audio src>` 读得到同目录文件。用 `canplaythrough` / `error` 事件探测可用性。

---

## 六、调参指南

| 想改什么 | 改哪里 |
|---|---|
| 整体音量 | `cfg.vol`（0~1，默认 0.75） |
| 某条音效太吵 | 该配方里每个 `hit/note` 的 `g:`（增益），或整条的 `note(... {g:0.2})` |
| 某条音效太长/太短 | `dur:`（撞击声）或最后一个参数（乐音时长） |
| 音高（"木"还是"瓷"） | `hit(t, 频率, {...})` 的第一个频率参数：越高越"瓷/脆"，越低越"闷/木" |
| 动作音需要更"重" | 加一次 `hit(t, 400, {type:'lowpass', g:.35})` 低频冲击 |
| 想加一条新音效 | 在 `R` 里加一个 key，然后在事件函数里 `SFX.play('新名字')`（`SFX.names()` 会自动包含它） |
| 提示音的时机 | `play(name,{delay:秒})`，例如 `SFX.play('chicken',{delay:0.75})` |

---

## 七、怎么验证（不是"点了有声音"就算数）

新增了专门的自动化测试 `tests/sound-test.js`（headless Edge + CDP），**35 项断言**：

```bash
node tests/sound-test.js "file:///D:/MyProjects/gymj/index.html"
```

它回答三个截图和耳朵都答不了的问题：

1. **引擎在不在** —— WebAudio 可用、23 条配方齐全、按钮已插入；
2. **真的出声了吗** —— 包住 `AudioContext.createOscillator/createBufferSource` 计数，
   **每条音效排出的音频节点数必须与配方一致**（例如"碰"=3 个撞击声、自摸=7 个乐音）。
   只说"调用没抛错"是不够的：配方里参数写错、数组越界都会被 `try/catch` 吞掉，
   听起来只是"变小声了"，肉眼和耳朵都发现不了；
3. **挂点接对了吗** —— 把 `SFX.play` 换成记录器，真跑一遍牌局流程
   （摇色子/发牌/摸打/碰杠胡/抢杠/叫牌/荒庄），断言每个事件都发出**该发的那一声**，
   并且"胡 → 翻鸡 → 结算"三个音的延迟是 **0 / 0.75 / 1.1 秒**（错开的）。

另外这几套回归也要一起跑（音效改动不该影响它们）：

| 测试 | 命令 | 断言数 |
|---|---|---|
| 逻辑自测 | `node tests/game-logic-test.js "file:///.../index.html"` | 88 项 |
| 布局回归 | `node <html-layout-verify>/verify-layout.js "file:///.../index.html"` | 45 场景 |

> ⚠️ **布局验证工具会在 `%TEMP%` 留下 `_edge_verify_*` 目录（每个 ~430MB）**，
> 跑多了会把 C 盘写满（实测把 C 盘写到 0 可用，连命令都执行不了）。
> 定期清理：`Remove-Item "$env:TEMP\_edge_verify_*" -Recurse -Force`。

---

## 八、当前状态一览

| 项 | 值 |
|---|---|
| 音效条数 | 23（实际挂到事件上的 20 条 + 3 条分支变体） |
| `SFX.play` 调用点 | 30 处 |
| 需要的音效资源文件 | **0 个**（全部实时合成；换成录音见第五节） |
| 浏览器支持 | Chrome / Edge / 小米浏览器 / UC / 微信 X5（有 WebAudio 即可；不支持时按钮自动隐藏） |
| 静音状态 | 存 `localStorage['gymj.sfx.v1']` |
| 新增测试 | `tests/sound-test.js`（35 项） |
