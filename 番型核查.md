# 番型（胡牌类型）核查报告

> 依据：你给的规则表 vs 代码实测值（`tests/hu-type-test.js` + `tests/extra-rules-test.js` 逐条打印）。
> **当前状态：表内 9 个番型全部核对一致，表外 4 个（大吊车/报听/天听/开局跟）已补做。**

---

## 一、🔴 已修好的 bug：清一色 / 清大对 判错了

**你的判据**：清一色、清大对必须**手牌和副露**都是同一门花色。

**旧实现的错**：`huPattern()` 先把副露那几张从牌里扣回去、**只用暗牌**判是否同花色，
于是"手里全万 + 副露是筒"也被判成清一色/清大对 —— 你看到的"大对子被写成清大对"就是这个。

```js
// 旧（错）：扣掉副露后只判暗牌
const dark=c.slice(); mc.forEach(card=>{ if(dark[card]>=3)dark[card]-=3; });
const one=isOneSuit(darkUsed>0?dark:c);
// 新（对）：判整副牌（fullCount = 手牌 + 每副副露 3 张）
const one=isOneSuit(c);
```

**修复后实测**（`tests/hu-type-test.js` ①组）：

| 手牌 | 副露 | 判出 |
|---|---|---|
| 1万刻 2万刻 3万刻 4万对 | **3筒碰** | ✅ **大对子**（修复前：清大对 ❌） |
| 同上 | 6万碰（也是万） | ✅ 清大对 |
| 123/456/789万 + 11万对 | **3筒碰** | ✅ **平胡**（修复前：清一色 ❌） |
| 同上 | 9万碰 | ✅ 清一色 |

> 本作**没有"吃"**，所以所有副露都是碰/杠（刻子），不存在"顺子副露"。

---

## 二、番值表（**已确认，与实现完全一致**）

你 2026-09-15 给的表（含自摸 +1 后的实测值）：

| 番型 | 番值 | 实测 | 状态 |
|---|---|---|---|
| 天胡 | 33 | 33（自摸后 34） | ✅ 一致 |
| 地胡 | 23 | 23（自摸后 24） | ✅ 一致 |
| 清龙七对 | 20 | 20 | ✅ 一致 |
| 清七对 | 17 | 17 | ✅ 一致 |
| 清大对 | 15 | 15 | ✅ 一致 |
| 龙七对 | 10 | 10 | ✅ 一致 |
| 清一色 | 10 | 10 | ✅ 一致 |
| 七对 | 7 | 7 | ✅ 一致 |
| 大对子 | 5 | 5 | ✅ 一致 |
| 平胡 | （表外） | 1 | — |

**这张表已写进代码**（`huPattern` 下方 + `BIG_FAN` 旁的长注释），并有 9 条断言逐条钉死 ——
以后谁改动番值，测试立刻会红。

> ⚠️ `BIG_FAN`（无豆点炮胡的门槛）= 5，必须等于"大对子"。改这张表要同步改它。

### 判定逻辑（与番值无关的部分）✅ 已逐条验过

| 项 | 结果 |
|---|---|
| 大对子 = 4 刻子 + 1 对（无顺子） | ✅ |
| 七对；**四个相同算两个对子** | ✅ `sevenPairsInfo` 输出 `{pairs:5, quads:1}` |
| 龙七对 = 七对 + **至少 1 组**四归一（多个四归一同档） | ✅ |
| 有副露时**不可能**是七对 | ✅ |
| 清一色须含副露 | ✅ **本次修复** |
| 天胡 = **庄家**首摸自摸；第二次摸牌不算 | ✅ |
| 地胡 = **非庄家**首摸自摸；第二次摸牌 / 点炮不算 | ✅ |

---

## 三、🆕 本次补做的 4 个番型

| 番型 | 番值 | 实现要点 |
|---|---|---|
| **大吊车** | **17** | 4 副露 + 单钓（手里只剩一对将）。本作没有吃，4 副露必然都是刻子 → 形状与大对子重合，靠"副露数 = 4"区分。清一色的也按 17（表里无"清大吊车"档） |
| **报听** | **17** | 见下方机制 |
| **天听** | **34** | 同上，只是记账名不同（闲家首摸即报听 = 天听） |
| **开局跟** | **4** | 庄家本局第一张打出的牌，其余三家各自的**第一张**弃牌都跟打同一张 |

### 报听 / 天听 的机制（按百科原文 + 你的番表落地）

窗口：**本局该玩家第一次摸完牌、还没出过牌**的那一刻（14 张），且"打掉一张就成听"。
之后：

| 约束 | 实现位置 |
|---|---|
| **不能换牌**（摸什么打什么） | `drawTile` 里报听者自动打出摸到的牌（只有能自摸时才停下来让他点「胡」）→ 手牌形状固定，听牌型不会被换掉 |
| **不能碰、不能杠** | `canMelded()` 最前面直接 return false（UI 按钮、AI 判定、结算前的合法性检查全跟着关） |
| 番值 | 结算时按 报听 17 / 天听 34 与牌型番**取最高** |
| 报听者打出的牌，别人可**无豆平胡** | `canHu` 增加 `force` 参数，`checkClaims` 里按"打牌者是否报听"传入 |

**记账名的分法**（两个来源有冲突，这里按**你的表**）：

| 情形 | 记作 | 番值 |
|---|---|---|
| 闲家（非庄家）第一次摸牌就报听 | **天听** | 34 |
| 庄家开局手牌就报听 | **报听** | 17 |

> 你的表注明"庄家不可天听"，所以庄家走报听 17。
> ⚠️ 百科《贵阳捉鸡》的口径是「报听者的牌**一律按清一色 10 番**计」——
> 若要用这个口径，把 `BAOTING_FAN / TIANTING_FAN` 两个常量改成 `10 / 10` 即可（一行）。

### 开局跟 的收付（⚠️ 方向是推断）

规则原文只写了**触发条件**，没写给谁多少。本实现取"跟打的三家从**庄家**各收 4 分"（庄家共付 12）。
若该反过来（三家各付庄家 4），把 `settle` 里那两行的 `±` 对调即可。

---

## 四、仍未定的事项

| # | 事项 | 现状 |
|---|---|---|
| 1 | **番型是"叠加"还是"取最高"？** | 现在是**取最高**（清一色大对子 = 15/17，不再 +5/+12）。你的公式写的是相加 → 这条不定，计分核心不动 |
| 2 | 「**杠 4 点**」与「**豆**（闷豆2/爬坡豆3/点豆1）」是两笔还是同一笔？ | 现在只有豆，没有"杠 4 点" |
| 3 | **乌骨鸡**是否参与冲锋鸡/责任鸡？ | 现严格只认幺鸡（开关 `CHARGE_ANY_CHICKEN`） |
| 4 | **开局跟的给付方向** | 推断为"庄家付"（见上） |
| 5 | **报听口径** | 按你的表 17/34（百科版为按清一色 10 番） |

---

## 五、怎么复现核查

```bash
node tests/hu-type-test.js "file:///D:/MyProjects/gymj/index.html"      # 番型判定与"清"的边界（20 项）
node tests/extra-rules-test.js "file:///D:/MyProjects/gymj/index.html"  # 番值表钉死 + 大吊车/报听/天听/开局跟
```
两个脚本都会把每个番型的**判定结果 + 番值**逐条打印出来，改番表后跑一遍即可看出连带影响。
