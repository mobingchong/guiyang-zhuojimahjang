# ===== 仓库只放「运行游戏需要的文件」=====
#   index.html · manifest.webmanifest · icon-192.png · icon-512.png
# 文档 / 测试 / 截图 / 素材 **只留在本地**，不入库（用户 2026-09-15 要求）。
# 想重新入库：删掉对应的忽略行，再 git add -f <文件>。

# 文档：只放 README（玩法规则说明）；其它专题 md（音效/语音/规则对比）不入库
*.md
!README.md

# 开发与验收产物
tests/
preview/

# 牌面素材（游戏运行时不读，只用于校准图形）
flat_tiles/
flat_tiles_png/
麻将数牌_扁平版.*

# 工具与工程目录（浏览器调试 profile 等，动辄上万文件）
.workbuddy/
node_modules/

# 牌面数据的生成工具 + 素材源 + 用户下载的素材仓库（都不参与运行，只用于重做牌面）
#   重做牌面的流程：tools/build-tile-faces.js → tools/install-tile-faces.js →
#   tools/verify-tile-geometry.js，素材在 素材/ 与 mahjong-tiles-main/
tools/
素材/
mahjong-tiles-main/

# 临时脚本、临时目录与备份（本项目约定：所有临时产物一律以 _ 开头）
_*
*.bak

